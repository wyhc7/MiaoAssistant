v1.1.1 反馈问题修复

## 修复
- **实时模式下颜文字不显示**：原先实时处理时颜文字被抑制到发送瞬间才追加，常因写入竞态丢失。
  现改为每条消息锁定同一枚颜文字，打字过程即稳定显示，不再依赖发送时机

## 改进
- 默认句末追加文本由「喵」改为「喵~」（已有配置不受影响，可在设置中修改）
- 输入波浪线（~ / ～）现在也会触发改写
- 新增应用屏蔽自检：当检测到目标应用隐藏界面内容（如微信对第三方无障碍的限制）时，
  在设置页顶部给出明确提示，避免"静默失效"
- 移除调试日志，恢复正式版行为

## 说明
- 微信 8.0.6x+ 对第三方无障碍服务隐藏视图树，属微信侧安全策略，本工具无法绕过；
  QQ / 抖音不受影响。设置页会自动检测并提示该状态
